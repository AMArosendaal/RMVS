// This file will contain all declarations (not definitions)

// Header guard. Ensures this library description is only included once
#ifndef RMVS_H
#define RMVS_H

int addFunction(int, int);
float analogPressure;
float 

#endif
